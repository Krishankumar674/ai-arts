# Fooocus AI Setup Guide

Follow these steps to install and use Fooocus AI for the competition:

---

## 🛠️ **Installation**

1. **Clone the Fooocus Repository**:
   - Open your terminal or command prompt.
   - Run the following command to clone the Fooocus repository:
     ```bash
     git clone https://github.com/lllyasviel/Fooocus.git
     ```

2. **Install Dependencies**:
   - Navigate to the Fooocus folder:
     ```bash
     cd Fooocus
     ```
   - Install the required dependencies by running:
     ```bash
     pip install -r requirements.txt
     ```

3. **Download Pretrained Models**:
   - Fooocus requires pretrained models to generate images. Download the models as instructed in the repository's README file.

---

## 🖼️ **Generating Artwork**

1. **Run Fooocus**:
   - Start Fooocus by running:
     ```bash
     python launch.py
     ```
   - This will open a web interface in your browser.

2. **Enter a Prompt**:
   - In the web interface, enter a text prompt describing the artwork you want to generate.
   - Example prompt: *"A futuristic city with flying cars and neon lights."*

3. **Generate and Save**:
   - Click the "Generate" button to create your artwork.
   - Once generated, save the image to your computer.

---

## ❓ **Troubleshooting**

- If you encounter any issues, refer to the [Fooocus GitHub repository](https://github.com/lllyasviel/Fooocus) for documentation and support.
- For additional help, open an issue in this competition repository or contact us at `ai-art-competition@example.com`.

---

✨ **You're all set! Start creating your masterpiece!** ✨
